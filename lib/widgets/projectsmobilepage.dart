import 'package:flutter/material.dart';

class Projectsmobilepage extends StatelessWidget {
  const Projectsmobilepage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
