import 'package:flutter/material.dart';

class Contactsmobilepage extends StatelessWidget {
  const Contactsmobilepage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
